# Sistema operativo
.DS_Store
Thumbs.db
desktop.ini

# Editor
.vscode/

# Netlify (carpeta local de builds/cache, no la config)
.netlify/

# Por si en el futuro se agrega tooling de build
node_modules/

# Graphify: cache y metadata específica de esta máquina (rutas absolutas,
# no portables entre clones). El grafo en sí (graph.html/json, GRAPH_REPORT.md,
# manifest.json) sí se versiona.
graphify-out/cache/
graphify-out/.graphify_python
graphify-out/.graphify_root
graphify-out/cost.json
graphify-out/[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]/

# Impeccable: preferencias por desarrollador y estado local de sesiones de
# /impeccable live (no es config de proyecto compartida). design.json y
# live/config.json sí se versionan.
.impeccable/config.local.json
.impeccable/live/sessions/
.impeccable/assets/
