# Performance

Pendiente de revisar:

- Imágenes sin optimizar (varias por encima de 500KB, ver `assets/portada-home/` e `historia/`).
- Falta `loading="lazy"` en imágenes fuera del viewport inicial.
- Falta `srcset` para variantes mobile.

**No usar Git LFS para esto** — la solución es optimizar/comprimir las imágenes antes de subirlas, no cambiar cómo se versionan.
